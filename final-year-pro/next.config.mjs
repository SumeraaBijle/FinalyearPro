/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['hebbkx1anhila5yf.public.blob.vercel-storage.com'], // Add the allowed hostname here
    },
  };
  
export default nextConfig;
